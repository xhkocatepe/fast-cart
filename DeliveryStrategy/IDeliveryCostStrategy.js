class IDeliveryCostStrategy {
    /**
     *  Calculating delivery cost depends on delivery algorithms.
     */
    calculateDeliveryCost(numberOfProducts) {
    }
}

module.exports = IDeliveryCostStrategy;
